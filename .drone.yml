---
kind: pipeline
type: docker
name: deploy-sergio.am

steps:
- name: build
  image: ubuntu:latest
  commands:
    - apt -y update && apt -y install bash jq pandoc
    - bash ./build.sh
    - rm -rf /var/www/_site && mv _site /var/www/

when:
  branch:
  - main

node:
  location: nexus
